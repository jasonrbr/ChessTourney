services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: chesstourney
      POSTGRES_USER: chesstourney
      POSTGRES_PASSWORD: chesstourney
    ports:
      - '5432:5432'
    volumes:
      - chesstourney-postgres:/var/lib/postgresql/data

volumes:
  chesstourney-postgres:
